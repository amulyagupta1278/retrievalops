apiVersion: v1
kind: Service
metadata:
  name: retrievalops-stable
spec:
  selector:
    app.kubernetes.io/name: retrievalops-api
  ports:
    - name: http
      port: 80
      targetPort: http
---
apiVersion: v1
kind: Service
metadata:
  name: retrievalops-canary
spec:
  selector:
    app.kubernetes.io/name: retrievalops-api
  ports:
    - name: http
      port: 80
      targetPort: http
